# Frontend (optional local helper)
API_BASE_URL=

# Core Lambda env
AWS_REGION=us-east-1
NOVA_MODEL_ID=nova-lite-v1
ALLOWED_ORIGIN=https://your-cloudfront-domain.example

# AUTO control plane state
WORKSPACE_BUCKET=auto-workspaces-example
RUNS_TABLE=auto-runs-example
CODEBUILD_PROJECT=

# Optional API auth (recommended)
AUTO_ADMIN_TOKEN=replace_with_random_long_token

# Optional channel stub
WHATSAPP_VERIFY_TOKEN=replace_with_verify_token
